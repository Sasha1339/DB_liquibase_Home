databaseChangeLog:
  - changeSet:
      id: course
      author: sesh
      createTable:
        tableName: course
        columns:
          - column:
              name: id
              type: bigint
              constraints:
                nullable: false
                primaryKey: true
          - column:
              name: name
              type: varchar(250)